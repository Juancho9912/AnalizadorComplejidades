function bsearch(A, n, key)
begin
    if (A[mid] = key) then
    begin
        return mid;
    end
    else
    begin
        if (A[mid] < key) then
            low <- mid + 1;
        else
            high <- mid - 1;
        end
    end
end
